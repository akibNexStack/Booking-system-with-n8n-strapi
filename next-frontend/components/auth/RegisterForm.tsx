"use client";

import Link from "next/link";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { Check, Chrome, Eye, EyeOff, Github, LockKeyhole, Mail, ShieldCheck, Sparkles, UserRound } from "lucide-react";
import { AuthShell } from "@/components/auth/AuthShell";
import { useAuthStore } from "@/store/useAuthStore";

export function RegisterForm() {
  const router = useRouter();
  const { register, isLoading, error } = useAuthStore();
  const [step, setStep] = useState<1 | 2>(1);
  const [showPassword, setShowPassword] = useState(false);
  const [formError, setFormError] = useState("");
  const [details, setDetails] = useState({ firstName: "", lastName: "", email: "", phone: "" });

  function continueToSecurity(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    const data = new FormData(event.currentTarget);
    const nextDetails = { firstName: String(data.get("firstName")), lastName: String(data.get("lastName")), email: String(data.get("email")), phone: String(data.get("phone")) };
    if (!nextDetails.firstName || !nextDetails.lastName || !nextDetails.email || !nextDetails.phone) { setFormError("Please complete all personal details."); return; }
    setDetails(nextDetails); setFormError(""); setStep(2);
  }
  async function createAccount(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    const data = new FormData(event.currentTarget);
    const password = String(data.get("password"));
    if (password.length < 8) { setFormError("Password must contain at least 8 characters."); return; }
    if (password !== String(data.get("confirmPassword"))) { setFormError("Passwords do not match."); return; }
    try { await register(`${details.firstName} ${details.lastName}`, details.email, password); router.push("/my-bookings"); } catch { /* Store has error text. */ }
  }

  const inputClass = "w-full rounded-xl border border-slate-200 bg-slate-50 py-3 pl-11 pr-4 text-sm text-slate-900 outline-none transition placeholder:text-slate-400 focus:border-rose-500 focus:bg-white focus:ring-4 focus:ring-rose-500/10";
  return <AuthShell mode="register"><div className="w-full"><span className="inline-flex items-center gap-1.5 rounded-full border border-rose-200 bg-rose-50 px-3 py-1 text-xs font-medium text-rose-600"><Sparkles size={13} />New Account</span><h2 className="mt-5 text-3xl font-bold tracking-tight text-slate-900 dark:text-white">Create your account</h2><p className="mt-2 text-sm text-slate-500">Already have an account? <Link href="/login" className="font-semibold text-rose-600 hover:text-rose-700">Sign in</Link></p><div className="mt-8 flex items-center gap-3"><div className="flex items-center gap-2 text-sm font-medium text-rose-600"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-rose-600 text-white">{step === 2 ? <Check size={16} /> : "1"}</span>Personal Info</div><div className="h-px flex-1 bg-slate-200" /><div className={`flex items-center gap-2 text-sm font-medium ${step === 2 ? "text-rose-600" : "text-slate-400"}`}><span className={`flex h-8 w-8 items-center justify-center rounded-full ${step === 2 ? "bg-rose-600 text-white" : "bg-slate-100"}`}>2</span>Security</div></div>{step === 1 && <><div className="mt-8 grid grid-cols-2 gap-3"><button type="button" className="flex items-center justify-center gap-2 rounded-xl border border-slate-200 bg-slate-50 py-3 text-sm font-medium text-slate-600"><Chrome size={18} />Google</button><button type="button" className="flex items-center justify-center gap-2 rounded-xl border border-slate-200 bg-slate-50 py-3 text-sm font-medium text-slate-600"><Github size={18} />GitHub</button></div><div className="my-8 flex items-center gap-4 text-[11px] font-medium uppercase tracking-wider text-slate-400 before:h-px before:flex-1 before:bg-slate-200 after:h-px after:flex-1 after:bg-slate-200">or register with email</div><form onSubmit={continueToSecurity} className="space-y-5"><div className="grid grid-cols-2 gap-4"><label className="text-sm font-medium text-slate-700">First Name<span className="relative mt-2 block"><UserRound size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" /><input name="firstName" defaultValue={details.firstName} placeholder="John" className={inputClass} /></span></label><label className="text-sm font-medium text-slate-700">Last Name<input name="lastName" defaultValue={details.lastName} placeholder="Doe" className={`${inputClass} mt-2 pl-4`} /></label></div><label className="block text-sm font-medium text-slate-700">Email Address<span className="relative mt-2 block"><Mail size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" /><input name="email" type="email" defaultValue={details.email} placeholder="john@example.com" className={inputClass} /></span></label><label className="block text-sm font-medium text-slate-700">Phone Number<span className="relative mt-2 block"><UserRound size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" /><input name="phone" type="tel" defaultValue={details.phone} placeholder="+1 (555) 000-0000" className={inputClass} /></span></label><button type="submit" className="flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-rose-500 to-rose-700 px-4 py-3.5 text-sm font-semibold text-white shadow-lg shadow-rose-500/25">Continue <span aria-hidden>→</span></button></form></>}{step === 2 && <form onSubmit={createAccount} className="mt-8 space-y-5"><div className="rounded-xl border border-rose-100 bg-rose-50 p-4 text-sm text-rose-700"><ShieldCheck size={17} className="mr-2 inline" />Your details are protected and used only for your appointments.</div><label className="block text-sm font-medium text-slate-700">Password<span className="relative mt-2 block"><LockKeyhole size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" /><input name="password" type={showPassword ? "text" : "password"} placeholder="At least 8 characters" onChange={() => setFormError("")} className="w-full rounded-xl border border-slate-200 bg-slate-50 py-3 pl-11 pr-11 text-sm text-slate-900 outline-none focus:border-rose-500 focus:ring-4 focus:ring-rose-500/10" /><button type="button" onClick={() => setShowPassword((value) => !value)} className="absolute right-3 top-1/2 -translate-y-1/2 p-1 text-slate-400">{showPassword ? <EyeOff size={18} /> : <Eye size={18} />}</button></span></label><label className="block text-sm font-medium text-slate-700">Confirm Password<span className="relative mt-2 block"><LockKeyhole size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" /><input name="confirmPassword" type={showPassword ? "text" : "password"} placeholder="Repeat your password" onChange={() => setFormError("")} className="w-full rounded-xl border border-slate-200 bg-slate-50 py-3 pl-11 pr-4 text-sm text-slate-900 outline-none focus:border-rose-500 focus:ring-4 focus:ring-rose-500/10" /></span></label><div className="flex gap-3"><button type="button" onClick={() => setStep(1)} className="rounded-xl border border-slate-200 px-5 py-3 text-sm font-semibold text-slate-600">Back</button><button type="submit" disabled={isLoading} className="flex flex-1 items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-rose-600 to-rose-800 px-4 py-3.5 text-sm font-semibold text-white shadow-lg shadow-rose-600/25 disabled:opacity-60">{isLoading ? "Creating account..." : "Create Account"}<span aria-hidden>→</span></button></div></form>}{(formError || error) && <p role="alert" className="mt-5 rounded-xl bg-rose-50 px-4 py-3 text-center text-sm text-rose-600">{formError || error}</p>}</div></AuthShell>;
}
